# Рецепт: {Название}

## Назначение

Что делает этот рецепт и зачем его использовать.

## Предварительные требования

- [ ] Levara-сервер запущен (`curl http://localhost:8080/health`)
- [ ] Коллекция установлена (`set_context(collection="...")`)
- [ ] {Дополнительные требования}

## Шаги

1. **Шаг первый** — что делать и зачем
   ```
   tool_call(...)
   ```

2. **Шаг второй** — следующее действие
   ```
   tool_call(...)
   ```

## Smoke-тест

Как проверить что всё сработало:

```
search(search_query="что-то из загруженных данных")
```

Ожидание: минимум N результатов с релевантным содержимым.

## Откат

Как отменить если что-то пошло не так:

- Удалить конкретный dataset: `delete(dataset_id="...")`
- Ядерный вариант: `prune()` (удаляет всё)

## Заметки

Подводные камни, соображения по производительности или советы.
