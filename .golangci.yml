version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - ineffassign
    - unused
  exclusions:
    paths:
      - '^webui/node_modules/'
      - '^node_modules/'
    rules:
      - path: _test\.go
        linters:
          - unused
