# Billing API Contract

## Endpoints

The billing OpenAPI contract defines `/invoices`, `/payment-methods`, and
`/refunds`. Client SDKs must pin the contract version used in production.

## Compatibility

Breaking endpoint changes require a new API version and a migration note in the
workspace.
